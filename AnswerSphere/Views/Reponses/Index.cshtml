@model IEnumerable<AnswerSphere.Models.ReponseOptionViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Reponse.RepondantNom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Option.OptionLibelle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Reponse.RepondantNom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Option.OptionLibelle)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Reponse.ReponseId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Reponse.ReponseId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Reponse.ReponseId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
