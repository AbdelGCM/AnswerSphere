@model IEnumerable<AnswerSphere.Models.OptionQuestionViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Option.OptionLibelle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Question.QuestionLibelle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Option.OptionLibelle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Question.QuestionLibelle)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Option.OptionId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Option.OptionId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Option.OptionId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
